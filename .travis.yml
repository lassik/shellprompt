sudo: no
language: c
compiler:
  - gcc
addons:
  apt:
    packages:
      - cabal-install
      - ghc
      - liblua5.2-dev
      - lua5.2
      - nodejs
install:
  - cabal install shellcheck
  - npm install eclint
before_script:
  - eclint .
  - shellcheck *.sh
script:
  - ./build-linux.sh
  - build-linux/shellprompt version
